<%# Error de la app. Mensaje en criollo, sin tecnicismos. %>
<div class="pantalla">
  <div class="cuerpo" style="padding:30px 20px 20px; gap:14px">
    <span class="etiqueta-ambar">Atención</span>
    <span class="t-pantalla-g"><%= titulo %></span>
    <p class="txt" style="margin:0; font-size:15px; line-height:1.5"><%= mensaje %></p>
    <div class="espaciador"></div>
    <a class="btn btn-grande" href="<%= volver %>">Volver</a>
  </div>
</div>
